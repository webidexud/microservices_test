<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              teal: '#5ea9a7',
              dark: '#23322d',
              gold: '#d6b770'
            },
            backgroundImage: {
              'gradient-auth': 'linear-gradient(135deg, rgba(94,169,167,0.9) 0%, rgba(35,50,45,0.95) 100%)',
            }
          }
        }
      }
    </script>
    <title>Upload Manager</title>
</head>
<body class="bg-gradient-auth bg-fixed min-h-screen font-sans">
    <div class="container mx-auto px-4 py-16 flex flex-col items-center">
        <!-- Formulario unificado -->
        <div class="w-full max-w-2xl bg-white/5 backdrop-blur-xl rounded-2xl overflow-hidden shadow-2xl border border-teal/20">
            <div class="bg-gradient-to-r from-teal to-dark p-6 text-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-gold" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                </svg>
                <h1 class="text-3xl font-bold text-gold mt-4">Carga tus archivos</h1>
                <p class="text-teal-100 mt-2">Sube los archivos PMO y/o Financiera</p>
            </div>

            <!-- Sección de instrucciones -->
            <div class="px-8 pt-6 pb-2 bg-dark/20">
                <div class="flex items-start bg-teal/10 border border-teal/20 rounded-lg p-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mt-0.5 mr-2 text-gold flex-shrink-0" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
                    </svg>
                    <div>
                        <p class="text-sm font-medium text-gold">Instrucciones importantes</p>
                        <p class="text-xs text-teal-100 mt-1">Debes cargar al menos un archivo (PMO, Giros ó de Ingresos). Puedes subir ambos si los tienes disponibles.</p>
                    </div>
                </div>
            </div>

            <!-- Formulario unificado con dos campos de archivo -->
            <form action="Dashboard" method="post" enctype="multipart/form-data" class="p-8 space-y-6" id="uploadForm">
                <!-- Campo para el archivo PMO -->
                <div class="space-y-2">
                    <label class="block text-sm font-medium text-teal-100">Archivo PMO <span class="text-xs text-teal-300">(Opcional)</span></label>
                    <div class="mt-1 flex rounded-md shadow-sm">
                        <input 
                            type="file" 
                            name="pmoFile" 
                            accept=".xlsx" 
                            class="flex-1 block w-full rounded-l-md border-0 bg-white/10 text-white file:mr-4 file:py-3 file:px-4 file:rounded-l-md file:border-0 file:text-sm file:font-semibold file:bg-teal file:text-dark hover:file:bg-teal/90 transition-colors focus:ring-2 focus:ring-gold focus:border-gold outline-none"
                            id="pmoFileInput"
                        >
                        <span class="inline-flex items-center px-3 rounded-r-md bg-dark/50 border border-l-0 border-teal/30 text-sm text-gold">
                            .xlsx
                        </span>
                    </div>
                </div>

                <!-- Campo para el archivo Financiera -->
                <div class="space-y-2">
                    <label class="block text-sm font-medium text-teal-100">Archivo Giros <span class="text-xs text-teal-300">(Opcional)</span></label>
                    <div class="mt-1 flex rounded-md shadow-sm">
                        <input 
                            type="file" 
                            name="financieraFile" 
                            accept=".xlsx" 
                            class="flex-1 block w-full rounded-l-md border-0 bg-white/10 text-white file:mr-4 file:py-3 file:px-4 file:rounded-l-md file:border-0 file:text-sm file:font-semibold file:bg-teal file:text-dark hover:file:bg-teal/90 transition-colors focus:ring-2 focus:ring-gold focus:border-gold outline-none"
                            id="financieraFileInput"
                        >
                        <span class="inline-flex items-center px-3 rounded-r-md bg-dark/50 border border-l-0 border-teal/30 text-sm text-gold">
                            .xlsx
                        </span>
                    </div>
                </div>

                <!-- Campo para el archivo de Ingresos -->
                <div class="space-y-2">
                    <label class="block text-sm font-medium text-teal-100">Archivo Ingresos <span class="text-xs text-teal-300">(Opcional)</span></label>
                    <div class="mt-1 flex rounded-md shadow-sm">
                        <input 
                            type="file" 
                            name="ingresosFile" 
                            accept=".xlsx" 
                            class="flex-1 block w-full rounded-l-md border-0 bg-white/10 text-white file:mr-4 file:py-3 file:px-4 file:rounded-l-md file:border-0 file:text-sm file:font-semibold file:bg-teal file:text-dark hover:file:bg-teal/90 transition-colors focus:ring-2 focus:ring-gold focus:border-gold outline-none"
                            id="ingresosFileInput"
                        >
                        <span class="inline-flex items-center px-3 rounded-r-md bg-dark/50 border border-l-0 border-teal/30 text-sm text-gold">
                            .xlsx
                        </span>
                    </div>
                </div>

                <!-- Mensaje de error (se mostrará si no se selecciona ningún archivo) -->
                <div id="errorMessage" class="hidden bg-red-100 border-l-4 border-red-500 text-red-700 p-3 rounded">
                    <div class="flex items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                        <p class="text-sm">Debes seleccionar al menos un archivo (PMO, Giros o de Ingresos)</p>
                    </div>
                </div>

                <!-- Botón de envío único -->
                <div>
                    <button 
                        type="submit" 
                        class="w-full flex justify-center items-center py-3 px-4 border border-transparent rounded-md shadow-sm text-lg font-bold text-dark bg-gold hover:bg-gold/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gold/50 transition-all transform hover:-translate-y-0.5"
                        id="submitButton"
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" />
                        </svg>
                        Procesar Archivos
                    </button>
                </div>
            </form>

            <div class="px-8 py-4 bg-dark/30 text-center border-t border-teal/10">
                <p class="text-xs text-teal-100">
                    Formatos soportados: XLSX (Excel 2007+)
                </p>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('uploadForm').addEventListener('submit', function(e) {
            const pmoFile = document.getElementById('pmoFileInput').files.length;
            const financieraFile = document.getElementById('financieraFileInput').files.length;
            const ingresosFile = document.getElementById('ingresosFileInput').files.length;
            const errorMessage = document.getElementById('errorMessage');
            
            if (!pmoFile && !financieraFile && !ingresosFile) {
                e.preventDefault(); // Evita el envío del formulario
                errorMessage.classList.remove('hidden');
                errorMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
            } else {
                errorMessage.classList.add('hidden');
            }
        });
    </script>
</body>
</html>